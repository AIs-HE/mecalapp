module.exports = {
    plugins: {
        // use the new Tailwind PostCSS adapter package (replaces using `tailwindcss` directly)
        '@tailwindcss/postcss': {},
        autoprefixer: {},
    },
}
